// Container params live here rather than in main.nf: nextflow.config is parsed
// before the script, so process.container cannot see script level params.
params.crversion    = "9.0.1"
params.container    = null

process.container   = params.container ?: "docker://ghcr.io/bixbeta/cellranger:${params.crversion}"

singularity.enabled     = true
singularity.autoMounts  = true
singularity.runOptions  = "-B /local -B /workdir"


process {
  withLabel:process_low {
    cpus   =    2
    memory =    14.GB

  }
  withLabel:process_medium {
    cpus   =    8
    memory =    32.GB

  }
  withLabel:process_high {
    cpus   =    24
    memory =    200.GB

  }

}


workflow.onComplete = {
    // any workflow property can be used here
    println "Pipeline complete"
    println "Command line: $workflow.commandLine"
}

workflow.onError = {
    println "Error: something went wrong"
}
